import AppKit
import SwiftUI

/// Shared mana accent: matches the menu bar progress bar fill (`#933ec5`).
enum ManaColors {
    static let bar = Color(red: 147 / 255, green: 62 / 255, blue: 197 / 255)
    static let barNS = NSColor(srgbRed: 147 / 255, green: 62 / 255, blue: 197 / 255, alpha: 1)
}
